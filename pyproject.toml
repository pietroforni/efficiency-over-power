[project]
name = "efficiency-over-power-tools"
version = "0.1.0"
description = "Development tools for analyzing LBM benchmark results"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
]

[tool.uv]
package = false
